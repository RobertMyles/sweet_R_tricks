---
title: "d3test"
author: "Robert Myles McDonnell"
date: "April 13, 2018"
output: html_document
---

```{r, results='asis'}
library(dplyr)
library(jsonlite)

send_df_to_js <- function(df){
  cat(
    paste(
    '<script>
      var data = ', toJSON(df),';
    </script>', sep = "")
  )
}


our_data <- data_frame(
  country = c("Denmark", "Belgium", "Croatia", "Germany", "Greece",
              "Italy", "Ireland", "Spain", "Switzerland", "France"),
  gdp = c(53400, 41000, 12100, 42000, 18100, 30500, 62000, 
          26000, 73000, 36000)
)

send_df_to_js(our_data)
```

So far, so good -- the data is in our browser, as you can see from opening the inspector on this page, going to the console, and typing `data` (since it's a JavaScript variable, defined with `var = data`).   


<style>

.bar {
  fill: steelblue;
}

.bar:hover {
  fill: brown;
}

.axis--x path {
  display: none;
}

</style>
<script src="//d3js.org/d3.v4.min.js"></script>
<div id = "viz"></div>
```{js}
// set the dimensions and margins of the graph
var margin = {top: 20, right: 20, bottom: 30, left: 40},
    width = 960 - margin.left - margin.right,
    height = 500 - margin.top - margin.bottom;

// set the ranges
var x = d3.scaleBand()
          .range([0, width])
          .padding(0.1);
var y = d3.scaleLinear()
          .range([height, 0]);

var svg = d3.select("#viz").append("svg")
    .attr("width", width + margin.left + margin.right)
    .attr("height", height + margin.top + margin.bottom)
  .append("g")
    .attr("transform", "translate(" + margin.left + "," + margin.top + ")")

x.domain(data.map(function(d) { return d.country; }));
y.domain([0, d3.max(data, function(d) { return d.gdp; })]);

svg.selectAll(".bar")
      .data(data)
    .enter().append("rect")
      .attr("class", "bar")
      .attr("x", function(d) { return x(d.country); })
      .attr("width", x.bandwidth())
      .attr("y", function(d) { return y(d.gdp); })
      .attr("height", function(d) { return height - y(d.gdp); });


svg.append("g")
    .attr("transform", "translate(0," + height + ")")
    .call(d3.axisBottom(x));

// add the y Axis
svg.append("g")
    .call(d3.axisLeft(y));
```